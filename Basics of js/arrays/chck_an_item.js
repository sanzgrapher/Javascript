const array = ['one', 'two', 'three', 'four'];

const hasValue = array.includes('five');

// check the condition
if (hasValue) {
    console.log('Array contains a value.');
} else {
    console.log('Array does not contain a value.');
}