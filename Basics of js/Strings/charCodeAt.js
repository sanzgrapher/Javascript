document.getElementById("title").innerHTML = `charCodeAt(); :: string.charCodeAt(index)`;

const name = 'naran';
// unicode of the desire character 
index = 4;
console.log(`The charcode of ${name.charAt(index)} is ${name.charCodeAt(index)}`);
// unicode of the last character 
console.log(name.charCodeAt(name.length-1))

